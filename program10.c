#include<stdio.h>
int main()
{
    int upper_limit,lower_limit,prime=1,i;
    printf("ENTER THE LOWER RANGE:");
    scanf("%d",&lower_limit);
    printf("ENTER THE UPPER RANGE :");
    scanf("%d",&upper_limit);
    
    for(i=lower_limit;i<=upper_limit;i++)
    {
           
    }
}